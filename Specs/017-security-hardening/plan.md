# Security hardening — untrusted file I/O audit (F33 pass) + follow-ups

| | |
|---|---|
| **Feature** | Cross-cutting security audit + follow-up tracker (no single roadmap feature; born from F33 review rounds 6–8) |
| **Status** | **AUDIT COMPLETE** — 2026-07-21. Severe tier fixed on `TRIAGE-COMMAND` (see [Specs/016 §10 round 9](../016-corpus-triage/plan.md)); this plan holds the **evidence table** (every read/write path, verdicted) and the **tracked follow-ups**. **Follow-up sweep (2026-07-22, `HARD-SECURITY`):** T2/T4/T6/T7/T9/T10/T11 fixed (§5); only the two *decisions* — T3 (repo-config executables) and T12 (corpus-integrity exit code) — remain, each deferred to its own round. |
| **Threat model** | Strictest (user-decided): hostile = everything in a cloned project folder (config values included), everything a recorded session produced, and operator-supplied flags/env paths. Trusted = the skillet binary and its own freshly written output. |
| **Method** | Risk-tiered full inventory (every `String(contentsOf:)`/`Data(contentsOf:)`/`FileHandle`/`contentsOfDirectory`/write primitive in `Sources/`), deep verification for hostile-bytes paths, inventory-line treatment for trusted-tier paths. Checklist: symlink (lstat-first), regular-file (FIFO/socket/device/dir), hard link (`linkCount > 1` — CWE-62), bounded read, binary reject, path confinement; writes: atomic (rename-over, never open a planted entry), no-overwrite, confined destination. Grounded: CERT FIO32-C, CWE-22/59/61/62, OWASP canonicalize-then-confine. |

## 1. What was fixed (this pass, on `TRIAGE-COMMAND`)

**The consolidation:** `SafeFile.readPlainData/readPlainText(url, cap:)` + typed `SafeReadRefusal` (each case carries its human phrase, so refusal wording can't drift) — **the one sanctioned untrusted read**. New-code rule: *file contents an outsider could control are read through this helper or not at all.*

**Severe tier (hang/escape on a default command path — the fix-here gate):**

| # | Path | Was | Now |
|---|---|---|---|
| S1 | `skillet.yaml` (every command, via `ConfigSupport`) | unguarded unbounded `String(contentsOf:)` behind a bare exists-check — a FIFO hung **every command**; `ConfigLoader.load(from:)` owned the read | read via helper (1 MiB); refusal → artifact error with the real reason; `ConfigLoader.load(from:)` **deleted** (seam is pure text→config again) |
| S2 | `--config <path>` (operator tier) | unbounded `String(contentsOfFile:)` | helper; refusal → usage error |
| S3 | `SKILL.md` + `evals.json` via `SkillReader` (lint/doctor default) | both unguarded + unbounded — a FIFO `SKILL.md` hung lint/doctor | helper (1 MiB / 8 MiB); SKILL.md refusal → environment error with reason; evals refusal → nil (never opened) |
| S4 | `trigger-eval.json` (doctor default, `TriggerEvalSupport`) | `Data(contentsOf:)` after symlink-only guards — FIFO hung the preflight | helper (4 MiB); refusal → `.invalid(reason:)` |
| S5 | `run`'s SKILL.md re-read (trigger-stub check) | unguarded read post-symlink-guard — FIFO hung pre-spend | helper; refusal → artifact error |
| S6 | `run`'s record I/O: prior `benchmark.json` read + `benchmark/grading/run.json` writes | unguarded prior read (FIFO hung **after spend**); non-atomic writes (open a planted FIFO → block; write through a planted hard link → clobber another inode) | prior via helper (refusal ⇒ absent); all three writes **atomic** (temp + rename — a planted entry is *replaced, never opened*: self-healing) |
| S7 | `project.skills_root` (every command's skill discovery) | unvalidated config value — `../../..` enumerated directories *outside* the project (names + skill-existence leak) before per-command confinement threw; reviewer-reproduced (round 10; was T8) | **accept-known-good at the config seam**: plain relative subpath only (reject absolute / `..` segments / empty), one rule inherited by lint/doctor/run/triage/capture; deeper guards stay layered (OWASP boundary validation; Project Zero patch-the-class). Reclassifies capture's hostile-root failure usage(2) → artifact(4) |
| S8 | `run --axis trigger` per-trial stub staging (`WorkspaceManager.prepareTrigger`) | unguarded `String(contentsOf: SKILL.md)` after symlink checks only — ran **per trial** after the once-per-run preflight, so a SKILL.md swapped for a FIFO in that window hung the run mid-spend (the TOCTOU variant; reviewer-caught round 11). **Audit miss:** the site was in round 9's inventory (`WorkspaceManager.swift:210`) but misjudged under a too-broad "✓ guarded" row — the pass's second corrected "swept" claim | safe reader (1 MiB); a refusal skips the stub exactly like a missing fence; regression test stages past a real FIFO |
| S9 | `SafeFile.boundedRead` size fallback (the shared primitive itself) | when the after-read `stat` failed (file deleted/replaced mid-race), `fullSize` fell back to `data.count` — which always passes callers' `fullSize <= cap` checks, so an oversized file could return **truncated as success** (round 11) | provable rule in `resolveFullSize`: verifiable size wins; an under-cap read hit EOF (exact count, no false refusal); a filled-cap read with no verifiable size reports just past the cap → every caller refuses (fail closed only where proof is impossible — CWE-367/CERT FIO45-C) |
| S10 | `SafeFile.boundedRead` open (the shared read primitive) | `FileHandle(forReadingFrom:)` after pre-open `stat` guards left a check-then-open (TOCTOU) window — a symlink swapped in at the final component after the lstat would be followed (round 16) | opens via POSIX `open(… O_RDONLY \| O_NONBLOCK \| O_NOFOLLOW)` → `FileHandle(fileDescriptor:)`: `O_NOFOLLOW` fails the open on a final-component symlink (closes that race by construction, CERT POS35-C), `O_NONBLOCK` stops a special-file-swap blocking; pre-open guards retained as the static layer, residual regular-file-swap documented as the accepted floor |
| S11 | `ScoreRunner` read (the deterministic scorers, round 17) | read via bare `SafeFile.boundedRead` with **no `linkCount` guard** — a hard-linked input (another inode, possibly outside the scored tree) was decoded and scored, its content influencing the reporter's output (T1) | migrated to `SafeFile.readPlainData`, the one sanctioned untrusted read, which refuses symlink / non-regular / `linkCount > 1` before the read; a hard link is now an S000 "not scored" disclosure (the single-file path, which `candidates` never guarded, is covered too). **Summary-count effect (INFO):** a hard-linked input now increments `filesUnreadable`, not `filesScored` — the intended effect of refusing it; an oversized file was counted `unreadable` before and after, so only the hard-link path shifted |
| S12 | `SafeFile.boundedRead` open (the shared read primitive) | after opening with `O_NONBLOCK \| O_NOFOLLOW` the descriptor's type was never re-checked — a special file (FIFO/socket/device) swapped in during the pre-check → open gap opened successfully and could be read as an empty file (sweep 2026-07-22) | `fstat` the **open descriptor** and refuse unless `S_ISREG` ([CERT FIO45-C](https://cmu-sei.github.io/secure-coding-standards/sei-cert-c-coding-standard/rules/input-output-fio/fio45-c/) — check the handle, not the path); closes the special-file TOCTOU by construction, the regular-file-for-regular-file swap stays the accepted floor |
| S13 | `SafeFile.noSymlink` (public confinement predicate) | isolated the sub-path by a raw `dropFirst(base.path.count)` string split with **no folder-boundary check** — a sibling like `database` vs `data` could be misparsed as confined (partial path traversal; latent — callers append subpaths) | reimplemented on the hardened `firstSymlinkOnPath` (realpath-canonical + boundary) plus the existing subtree scan — one escape-check implementation, inheriting the T9 soundness |
| — | `doctor` one-pass repair | a refused lint-input read aborted the whole report | refusal → a failure **row** (doctor's own never-fix-one-rerun contract) |
| — | `CorpusLoader` + `scanEvidence` (this branch's readers) | rounds 6–8 guards, copy-pasted | migrated onto the helper (policy stays local; mechanism shared) |

**Tests added:** helper refusal matrix incl. a **real FIFO** (provably refused instantly — the guard `stat`s before any `open`; the test hangs if the ordering ever regresses), directory/symlink/hard-link/missing/oversized/binary; binary-level no-hang regressions (FIFO `skillet.yaml` → lint exit 4 fast; FIFO `SKILL.md` → refused fast; hard-linked `skillet.yaml` → refused).

## 2. Evidence table — every read path

Tier: **H** = hostile bytes (deep-verified) · **T** = trusted/tool-owned (inventory). Verdict: ✓ guarded · **S#** fixed above · **T#** tracked (§4).

| Site | Reads | Tier | Verdict |
|---|---|---|---|
| `ConfigSupport` (was `ConfigLoader.load`) | `skillet.yaml`, `--config` | H | **S1/S2** |
| `ProjectKit/SkillReader` | `SKILL.md`, `evals.json` | H | **S3** |
| `skillet/TriggerEvalSupport` | `trigger-eval.json` | H | **S4** |
| `skillet/RunCommand:129` | `SKILL.md` (stub check) | H | **S5** |
| `skillet/RunCommand` records | prior `benchmark.json` | H | **S6** |
| `skillet/RunCommand` replay maps | `--replay-map` files (hidden test seam) | H (operator) | ✓ `readPlainData` (**T2** fixed) |
| `AnalysisKit/CorpusLoader` | bundle meta + SARIF | H | ✓ (r6/r8 + helper) |
| `skillet/TriageCommand.scanEvidence` | findings/friction `.md` | H | ✓ (r7/r8 + helper) |
| `ScoreKit/ScoreRunner` | scored tree (CLI path) | H | **S11** — `readPlainData`, hard link refused |
| `HarnessKit/ClaudeCodeAdapter` locate/export | session store JSONL | H | ✓ via shared `SafeFile.readPlainData` + predicates; private twin removed (**T10** fixed) |
| `HarnessKit/SkillBundleAudit` | directory names/metadata only | H | ✓ (no file opens; symlinks are its *subject*) |
| `RunKit/WorkspaceManager.copyFiltered` | staged skill tree | H | ✓ (skips symlinks + hidden at every depth) |
| `RunKit/WorkspaceManager.prepareTrigger` | `SKILL.md` per-trial stub read | H | **S8** (round 11 — round 9 misjudged it "guarded") |
| `RunKit/WorkspaceManager.snapshotStaged` | staged fixtures | H | ✓ regular+noSymlink; **bounded, fail-safe** (**T7** fixed) |
| `RunKit/WorkspaceManager.captureEvidence` | produced files | H | ✓ (F16: symlink, special, hard-link, bounded, disclosure) |
| `CorpusKit/BodyExtractor` | touched workspace files | H | ✓ via `readConfinedRegularText` (migrated round 17) |
| `ProjectKit/DirectoryProbe`, `SkillScanner` | directory listings | H | ✓ metadata-only; symlinked dirs not followed (verified r2) — `skills_root` value **S7** (fixed) |
| `CaptureCommand.scoreStaged` temp staging | writes/reads own temp | T | ✓ (keys pre-confined by `BodyExtractor`; invariant now **asserted** at the write — **T6** fixed) |
| `SafeFile` primitives | — | — | the mechanism itself; `firstSymlinkOnPath` **realpath-canonical** (**T9** fixed) |

## 3. Evidence table — every write path

| Site | Writes | Tier | Verdict |
|---|---|---|---|
| `RunCommand` records | `benchmark/grading/run.json` | H (dest in repo) | **S6** atomic |
| `CorpusKit/SessionBundleWriter` | bundle files | H (dest in repo) | ✓ atomic + collision-refuse + capture-side confinement |
| `TriageCommand` finding writes | `findings/*.md` | H (dest in repo) | ✓ confinement (r2) + `.withoutOverwriting` + collision disclosure + symlink dest check |
| `ProjectKit/InitExecutor` | scaffolding | H (dest in repo) | ✓ atomic; planner only plans absent files (never overwrites) |
| `RunKit/Runner` forensics | `.skillet/runs/<trial>/*` | T-ish (tool-created dirs; a cloned repo *can* ship `.skillet`) | ✓ **atomic** (**T4** fixed); path unguessable (`<ts>-<random-uuid>`) + prefix symlink-verified |
| `RunKit/WorkspaceManager` staging | temp workspace | T (fresh temp) | ✓ |
| `CaptureCommand.scoreStaged` | fresh UUID temp dir | T | ✓ (unpredictable path; keys pre-confined) |
| `WorkspaceManager.destroy` / cleanups | `removeItem` on tool-owned roots | T | ✓ |

## 4. Tracked follow-ups (deferred — milder than the fix-here gate)

| # | Severity | Item |
|---|---|---|
| T1 | ~~Low~~ **CLOSED (round 17)** | `ScoreRunner` now reads via `SafeFile.readPlainData`, which refuses `linkCount > 1` — a hard-linked input is disclosed as an S000 "not scored" instead of having its out-of-tree content decoded. See S11. |
| T2 | ~~Low~~ **FIXED (2026-07-22)** | `RunCommand` replay-map loaders now read via `SafeFile.readPlainData` (bounded + symlink/special/hard-link refused); a refusal falls back to the empty map exactly as before. |
| T3 | **High (design)** | **Executable paths from repo config are probed/executed** (`harness.claude-code.path`, `sanitize.scanner_path`): under the clone-and-run model, `skillet doctor` on a hostile repo executes a repo-chosen binary — arbitrary code execution by configuration. The fix is a *product decision* (workspace-trust prompt / ignore repo-pinned executable paths / first-use confirmation), not a guard — needs its own design round. Industry precedent: VS Code workspace trust, `git safe.directory`. |
| T4 | ~~Low~~ **FIXED (2026-07-22)** | `Runner` forensics writes are now atomic (temp + rename — a planted destination entry is *replaced*, never opened). Predictability analyzed benign: the cache root is `<timestamp>-<random-uuid>` (unguessable) and its `.skillet/runs` prefix is symlink-verified by `assertCacheNotSymlinked`, so pre-planting an entry isn't feasible. |
| T5 | Info | `InitExecutor`: verified guarded (row above) — no action. |
| T6 | ~~Info~~ **FIXED (2026-07-22)** | `scoreStaged` now re-asserts confinement at the write (`firstSymlinkOnPath(from: tmp, to: dest) == nil`) with the invariant documented — a future caller feeding unconfined keys can't write outside the temp tree. |
| T7 | ~~Low~~ **FIXED (2026-07-22)** | `snapshotStaged` reads **bounded** now (`StagedSnapshot`: size always kept, bytes held up to a 16 MiB cap). An over-cap file holds no bytes, so `unchanged` can't prove equality and **fails safe** — reported as changed, never a silently-skipped input. Exact byte comparison kept for in-cap files (no hash, no collision surface). |
| T8 | ~~Medium~~ | **FIXED (round 10)** — accept-known-good validator at the shared config seam (`SkilletConfig.Project.skillsRootViolation` + `ConfigSupport.validated`): `skills_root` must be a plain relative subpath (no absolute, no `..` segment, segment-wise); every command inherits it; capture's deeper guard retained as layered defense. See S7. |
| T9 | ~~Medium~~ **FIXED (2026-07-22)** | The escape test now compares in **confinement-canonical** form (`confinementCanonical`: `realpath` of the longest existing ancestor + the not-yet-created tail as text) on both sides — sound for a target being created, on every platform ([CERT FIO02-C](https://wiki.sei.cmu.edu/confluence/x/DtcxBQ)); `realpath` also catches a suffix symlink that resolves outside base. The raw-component symlink walk is retained. |
| T10 | ~~Medium~~ **FIXED (2026-07-22)** | The last hand-rolled reader is migrated: `ClaudeCodeAdapter` reads sessions via `SafeFile.readPlainData` and its store-locate filter via `SafeFile` predicates; the private twin is deleted (HarnessKit already depended on ProjectKit — no module-graph change). Every untrusted reader is now on the shared mechanism. |
| T11 | ~~Low~~ **FIXED (2026-07-22)** | Both `capture --date` and `triage --since` delegate to one shared `EDDCore.DateShape.isValidISODate` (shape-exact + calendar-valid); the two hand-rolled twins are gone. |
| T12 | Policy | Should a **corpus-integrity problem** (a file where a folder belongs; an all-unreadable corpus) affect `triage`'s exit code, and should such diagnostics render *above* the taxonomy? Triage is deliberately a reporter (exit 0 even with real failure clusters), yet a wrong-file-type is a *certain* error the confidence-severity principle says deserves a non-zero signal — a tension bigger than a bug-fix. Raised round 15; today it discloses + exits 0. |

## 5. Status log

- **2026-07-22 (review-findings round on `HARD-SECURITY` — 2 code hardenings + doc reconciliation).** Eight review findings triaged against the staged code (one — a `ScoreRunner` "oversized count changed" claim — was misattributed: oversized was counted `unreadable` before and after; the real shift is hard-linked inputs, now noted on S11). **S12** — the single sanctioned read (`boundedRead`) now `fstat`s the **open descriptor** and refuses a non-regular file, closing the special-file swap race by construction ([CERT FIO45-C](https://cmu-sei.github.io/secure-coding-standards/sei-cert-c-coding-standard/rules/input-output-fio/fio45-c/) — check the handle, not the path); the regular-file-swap stays the accepted floor (Unknown 1=A). **S13** — the public `noSymlink` predicate's raw string-prefix match (a partial-path-traversal footgun, latent today) was reimplemented on the realpath-canonical `firstSymlinkOnPath` + subtree scan — one escape-check, inheriting T9's soundness (Unknown 2=B). Docs reconciled: Specs/016 §8 (three now-done items marked fixed, pointing here); the AGENTS banner/modules/commands refreshed and its **pinned test count dropped** (a hand-maintained narrative shouldn't carry a volatile metric — Unknown 3=B); two stale code comments corrected (a claimed date round-trip the code no longer does; a refusal-case list naming cases the reader can't return). The two per-round green counts (587 at round 17, 592 at the sweep) are correct point-in-time snapshots, left intact. **+2 tests → 594 / 86 green, zero warnings, release clean.** Staged on `HARD-SECURITY`, no commit.
- **2026-07-22 (follow-up sweep on `HARD-SECURITY` — 7 tracked items cleared; the two decisions kept).** Scope decided via Q&A (option A: all code items now; T3/T12 to their own rounds). **T9** (Medium) — the confinement escape test was existence-dependent on Darwin (`standardizedFileURL` resolves `/private` only for existing paths, so a real base vs. a not-yet-created target compared in mismatched forms); replaced with `confinementCanonical` — `realpath` of the longest existing ancestor + lexical tail, both sides ([CERT FIO02-C](https://wiki.sei.cmu.edu/confluence/x/DtcxBQ)) — which also catches a suffix symlink that resolves outside base; raw-component walk retained. **T10** (Medium) — the last hand-rolled untrusted reader (`ClaudeCodeAdapter`'s private `boundedRead`/`isRegularFile`/`linkCount` twin) migrated to `SafeFile.readPlainData` + predicates and deleted (HarnessKit already deps ProjectKit — no module change); every untrusted reader is now on the shared mechanism. **T7** (Low) — `snapshotStaged` bounded via `StagedSnapshot` (size always kept, bytes ≤ 16 MiB); an over-cap file holds no bytes and **fails safe** (`unchanged` reports it changed, never a skipped input) — exact byte comparison kept in-cap, so no hash/collision surface ([why a hash gate is attackable](https://github.com/corkami/collisions)). **T4** (Low) — forensics writes atomic (temp+rename); predictability analyzed benign (`<ts>-<random-uuid>` root, prefix symlink-verified). **T2** (Low) — replay-map loaders → `readPlainData`. **T11** (Low) — `capture`/`triage` date checks unified into `EDDCore.DateShape`. **T6** (Info) — `scoreStaged` re-asserts key confinement at the write. **Remaining:** T3 (repo-config executables — product/trust decision) and T12 (corpus-integrity exit code — policy decision), each to its own round. **+5 tests → 592 / 86 green, zero warnings, release clean.** Staged on `HARD-SECURITY`, no commit.
- **2026-07-21 (round 11 — two audit escapes fixed; the evidence table corrected honestly).** (1) **S9 — the shared reader's own size check could lie**: with the after-read `stat` failed, the fallback reported "size = bytes read", auto-passing every caller's cap check → truncated-as-success. Fixed with the **provable rule** (chosen over the reviewer's blanket-refusal: an under-cap read provably hit EOF and needs no refusal; only a filled-cap read with unverifiable size fails closed — CWE-367/FIO45-C), extracted as `resolveFullSize` + unit matrix (the race itself can't be triggered from tests). (2) **S8 — the per-trial trigger-stub read** still used the raw call: FIFO swapped after preflight → mid-spend hang; now the safe reader, refusal ⇒ skip, tested with a real FIFO. **Process note:** the site *was* in round 9's inventory and was misjudged under a broad "✓ guarded" row — second corrected "swept" claim; the table now carries the narrow per-method rows. (nits) `SkillTriageResult` gains the public memberwise init its siblings have; the per-finding `createDirectory` hoisted to once per batch. Four reviewer-confirmed tracked items (T1/T2/T7/T9) unchanged. **+2 tests → 575 / 85 green, zero source warnings.** Staged, no commit.
- **2026-07-21 (round 10 — reviewer findings on the pass: 1 fixed-as-class, 1 display fix, 1 refuted, records corrected).** (1) **T8 promoted and fixed as S7** — the reviewer reproduced the enumeration leak live in triage; per the Q&A decision (option B, research-grounded: OWASP accept-known-good at the trust boundary; Project Zero comprehensive-fix — patching only the reproduced command is the classic incomplete-patch failure), the validator landed at the shared config seam for all commands at once; capture's test reclassified usage(2) → artifact(4) (a bad committed-config value is an artifact problem). (2) **Blocked-write rows told the truth already in JSON, now in the table too** — a "new" cluster whose write was refused rendered "new: …" beside a "not overwritten" disclosure; the FILE column now derives from the actual outcome (`blocked — … exists (see disclosure)`); machine format unchanged. (3) **"Hidden sibling gets read" — refuted structurally and empirically**: both of a stem's filenames share the stem prefix, so they share hiddenness; the planted hidden sibling has a different name than the loader constructs and is never referenced — regression test proves "missing" disclosure + no read. (4) **Record corrections**: the note claiming `ScoreRunner` "still uses unbounded `String(contentsOf:)`" is wrong — it has used `boundedRead` (1 MiB, regular-files-only) since F17; T1 remains solely the hard-link gap. T9 confirmed not triggered by triage (same-lexical-root construction) — stays tracked. **+4 tests** (validator matrix; blocked-row render; hidden-sibling refutation; hostile-`skills_root` refused across lint/doctor/triage at the binary level). **573 tests / 85 suites green, zero source warnings, release clean.**
- **2026-07-21 (audit + severe-tier fixes — one pass).** Q&A-scoped (5 unknowns: risk-tiered full inventory; strictest threat model incl. operator inputs; helper-now + severity-gated shipped fixes; writes in scope; evidence-table deliverable). Inventoried 28 read + ~46 write primitives; deep-verified every hostile-tier path. Fixed S1–S6 + the doctor one-pass repair + helper consolidation of the branch's readers; **+8 tests** (helper matrix incl. real-FIFO instant-refusal proof; 3 binary-level no-hang regressions). Tracked T1–T11 (T3 is the headline: repo-config executables — a design decision, not a guard). Suite: **569 tests / 85 suites green, zero source warnings; release (WMO) build clean.** Staged on `TRIAGE-COMMAND` with F33, no commit. Detail: [Specs/016 §10 round 9](../016-corpus-triage/plan.md).
